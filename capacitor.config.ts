import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brewbloomcoffee.app',
  appName: 'Brew Bloom Coffee',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
